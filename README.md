# sand-simulation
A reworked sand simulation and my first ever C project

## Build instructions
Make sure to have gcc, SDL3 and dependecies in your include folder
Type the following command in the terminal `gcc main.c -L<PATH_TO_YOUR_BIN_FOLDER> -lSDL3 -o main.exe`
